tasks:
  - name: CLI
    init: |
      cd ./upcli
      poetry install
      alias up='poetry run up'
    command: |
      echo "Ready at $(date)"

vscode:
  extensions:
    - ms-azuretools.vscode-docker
    - ms-python.python
